mixin phone(telNumber, className)
	a(href=`tel:+${telNumber.replace(/\D/g, '')}` class=['page__phone', className || null, 'd-inline-flex align-items-center'])
		+icon('phone', '', '20px', '20px')
		span.ms-2 #{telNumber}
